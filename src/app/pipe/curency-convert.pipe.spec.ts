import { CurencyConvertPipe } from './curency-convert.pipe';

describe('CurencyConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new CurencyConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
